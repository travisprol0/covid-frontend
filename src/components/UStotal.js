import React from 'react'

function UStotal(props) {
    return(
        <div className="UStotalData">
            <h5>Totals Since 01/22/2020</h5>
            {/* <p>Total Cases: {props.data.positive.toLocaleString("en")}</p>
            <p>Total Hospitalizations: {props.data.hospitalizedCumulative.toLocaleString("en")}</p>
            <p>Total ICU Patients: {props.data.inIcuCumulative.toLocaleString("en")}</p>
            <p>Lives Lost: {props.data.death.toLocaleString("en")}</p> */}
        </div>
    )
}

export default UStotal; 